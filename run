#!/bin/bash
hn=$(hostname)
./ccminer -a verus -o stratum+tcp://51.75.161.110:9999 -u ${PublicVerusCoinAddress}.${PoolWorker} -t ${VerusThreads} -N 20
#/mining/ccminer/ccminer -a verus -o stratum+tcp://${PoolHost}:${Port} -u ${PublicVerusCoinAddress}.${hn} -t ${VerusThreads}
